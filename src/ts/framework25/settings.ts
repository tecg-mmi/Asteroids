export const settings = {
    // default color value for HSL and RGB channels
    defaultColorValue: 0,
    // number of elements that are allowed even if they are no more used
    maxUnnecessaryAnimatablesItemCount:10
}